workflows:
  ios-workflow:
    name: iOS Build Workflow
    max_build_duration: 120
    environment:
      vars:
        BUILD_NUMBER: $BUILD_NUMBER
        CORDOVA_PATH: ios-app
      node: 20.11.0
      xcode: 15.4
      cocoapods: default
    scripts:
      - name: Install npm dependencies
        script: cd $CORDOVA_PATH && npm install
      
      - name: Install Cordova CLI
        script: npm install -g cordova
      
      - name: Add iOS platform
        script: cd $CORDOVA_PATH && cordova platform add ios || true
      
      - name: Install iOS dependencies
        script: cd $CORDOVA_PATH/platforms/ios && pod install
      
      - name: Build iOS Release
        script: cd $CORDOVA_PATH && cordova build ios --release --device
      
      - name: Export IPA
        script: |
          cd $CORDOVA_PATH
          mkdir -p build/ipa
          xcodebuild -workspace platforms/ios/App.xcworkspace \
            -scheme App \
            -configuration Release \
            -archivePath build/App.xcarchive \
            archive
          xcodebuild -exportArchive \
            -archivePath build/App.xcarchive \
            -exportPath build/ipa \
            -exportOptionsPlist platforms/ios/exportOptions.plist
    artifacts:
      - ios-app/build/ipa/*.ipa
      - ios-app/build/App.xcarchive